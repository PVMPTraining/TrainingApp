'use client'

import { useEffect, useState } from 'react';

export default function Account() {
	// const [data, setData] = useState(null);

	// useEffect(() => {
	//   // Make a request to the middleware-like API route
	//   fetch('/account/middleware')
	// 	.then((response) => response.json())
	// 	.then((data) => {
	// 	  // Handle the data returned from the API route
	// 	  setData(data);
	// 	});
	// }, []);

	return (
		<>
			<div>
				Account
			</div>
		</>
	)
}